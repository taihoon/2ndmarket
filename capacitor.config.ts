import { CapacitorConfig } from '@capacitor/cli';


const config: CapacitorConfig = {
  appId: 'kr.2ndmarket.dev',
  appName: '2ndmarket',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
